<?xml version="1.0" encoding="UTF-8"?>
<!--
in this example by sending:
cansend can0 203#0000000001000000
 we get the position.

the first 4 bytes of the 0x281 message are the position, the next byte is the current, and the last 2 bytes are status flags.

the first 4 bytes of the 0x201 message are the requested position.
forexample for the pos 1000, we have to send: (1000 = 0x03e8)
cansend can0 201#03e8000001000000

since the: 
configPdos="false"
startNode="false"

the index, subindex, and node id are ineffective, and the driver will not send the NMT start command to the node. The node must be started by some other means, for example by a separate script or by a different CANopen master.
-->
<socketcan interface="can0"
           periodUs="1000"
           syncPeriodUs="0"
           sdoTimeoutMs="500">
  <node name="motor"
        id="3"
        configPdos="false"
        startNode="false"
        heartbeatTimeoutMs="0">
    <rpdo number="1"
          cobId="0x201"
          transmissionType="255"
          periodMs="1"
          autoStart="false">
       <!-- read only the first 4 bytes for motor1 position--> 
      <pdoEntry name="target-position"
                index="0x2000"
                subIdx="0"
                bitLen="32"
                halType="s32"/>
    </rpdo>

    <rpdo number="2"
          cobId="0x203"
          transmissionType="255"
          periodMs="1">
      <!--skip the first 4 bytes-->
      <complexEntry bitLen="32"/>   
      <complexEntry name="request-enable"
                index="0x2011"
                subIdx="0"
                bitLen="16"
                halType="u32"
                bit0="enable-motor-1"
                bit1="enable-motor-2"
                />
    </rpdo>

    <tpdo number="1"
          cobId="0x281"
          transmissionType="255">
      <!--first 4 bytes are the position-->
      <pdoEntry name="actual-position"
                index="0x2100"
                subIdx="0"
                bitLen="32"
                halType="s32"/>
      <!--byte 5 is the current-->
      <pdoEntry name="actual-current"
                index="0x2101"
                subIdx="0"
                bitLen="8"
                halType="s32"/>
      <!-- bytes 6-7 are status flags-->
      <complexEntry name="status-flags"
                    index="0x2102"
                    subIdx="0"
                    bitLen="16"
                    halType="u32"
                    bit0="enable"
                    bit1="fault"
                    bit2="emergency"
                    bit3="home"
                    bit4="lim-cw"
                    bit5="lim-ccw"
                    bit6="homing"
                    bit7="homed"
                    bit8="brake"
                    bit9="out-dig"/>
    </tpdo>
  </node>
</socketcan>
